@model List<HelpdeskApp.Models.User>
@{
    ViewData["Title"] = "Users";
}

<div class="page-header">
    <div>
        <h4 class="page-title">Users</h4>
        <p class="page-subtitle">Manage team members and access control</p>
    </div>
    <a href="@Url.Action("Create")" class="btn btn-primary">
        <i class="bi bi-plus-lg me-1"></i> New User
    </a>
</div>

<div class="card">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>User</th>
                        <th>Email</th>
                        <th>Status</th>
                        <th>Joined</th>
                        <th style="width:100px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count == 0)
                    {
                        <tr><td colspan="5">
                            <div class="empty-state">
                                <i class="bi bi-people"></i>
                                <p>No users found yet.</p>
                            </div>
                        </td></tr>
                    }
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>
                                <div class="d-flex align-items-center gap-3">
                                    <div style="width:38px;height:38px;border-radius:10px;background:linear-gradient(135deg,#6366f1,#06b6d4);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.8rem;flex-shrink:0;">
                                        @{
                                            var parts = user.FullName.Split(' ');
                                            var i = parts.Length >= 2
                                                ? parts[0].Substring(0,1).ToUpper() + parts[^1].Substring(0,1).ToUpper()
                                                : user.FullName.Substring(0,1).ToUpper();
                                        }
                                        @i
                                    </div>
                                    <div>
                                        <div style="font-weight:700;color:var(--dark);font-size:0.9rem;">@user.FullName</div>
                                        <div style="font-size:0.78rem;color:var(--gray-3);">ID: @user.Id</div>
                                    </div>
                                </div>
                            </td>
                            <td style="color:var(--gray-2);">@user.Email</td>
                            <td>
                                @if (user.IsActive)
                                {
                                    <span class="badge badge-active"><i class="bi bi-check-circle me-1"></i>Active</span>
                                }
                                else
                                {
                                    <span class="badge badge-inactive"><i class="bi bi-x-circle me-1"></i>Inactive</span>
                                }
                            </td>
                            <td style="color:var(--gray-2);font-size:0.85rem;">@user.CreatedDate.ToString("MMM dd, yyyy")</td>
                            <td>
                                <form asp-action="ToggleActive" asp-route-id="@user.Id" method="post" style="display:inline;">
                                    @Html.AntiForgeryToken()
                                    @if (user.IsActive)
                                    {
                                        <button type="submit" class="btn btn-sm btn-ghost" title="Deactivate" style="background:rgba(239,68,68,0.08);color:#ef4444;">
                                            <i class="bi bi-toggle-on"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="submit" class="btn btn-sm btn-ghost" title="Activate">
                                            <i class="bi bi-toggle-off"></i>
                                        </button>
                                    }
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
